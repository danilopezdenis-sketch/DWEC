const nombre = "Daniel Alejandro López Denis"
let edad = 19
const tieneMascota = false

// const tieneMascota da error


let frase = `${nombre} tiene ${edad} años y ${tieneMascota ? "sí":"no"} tiene mascota`
console.log(frase)
